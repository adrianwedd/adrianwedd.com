# Sprint 30 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close out infographic regeneration, verify email delivery, harden the enquiry system, and set up multi-LLM fallback for resilience.

**Duration:** 1 session (~2-3 hours)

**Issues:** #216 (infographics), #217 (email routing), #213 (multi-LLM fallback)

---

## Phase 1: Finish & Verify (P0)

### Task 1: Complete infographic regeneration (#216)
- [ ] Create 9 remaining NLM notebooks (quota reset after 24h)
- [ ] Add sources, generate portrait infographics with dark brand focus
- [ ] Download PNGs, convert to WebP with `cwebp -q 85`
- [ ] Verify all 22 are 1536x2752
- [ ] Commit and push
- [ ] Close #216

**Items:** squishmallowdex, strategic-acquisitions, tel3sis, the-mitigation-gap, the-notebooklm-pipeline, the-resonant-fringe, the-robot-that-refuses-to-give-orders, veritas, when-ai-systems-talk-safety-breaks

### Task 2: Verify Cloudflare Email Routing (#217)
- [ ] Check CF dashboard: Email Routing enabled on adrianwedd.com?
- [ ] Verify MX records for CF Email Routing
- [ ] If not enabled: enable + configure verified destination address
- [ ] Test: submit enquiry form, check worker logs (`wrangler tail`)
- [ ] If email arrives: close #217
- [ ] If not: diagnose and fix (may need `send_email` binding config adjustment)

---

## Phase 2: Multi-LLM Fallback (#213)

### Task 3: Add provider abstraction to adrianwedd-ops

**Files:**
- Create: `worker/src/llm/provider.ts` — provider interface + factory
- Create: `worker/src/llm/anthropic.ts` — current Claude implementation extracted
- Create: `worker/src/llm/gemini.ts` — Gemini Flash/Pro via REST API
- Create: `worker/src/llm/openai.ts` — GPT-4.1-mini via REST API
- Modify: `worker/src/env.ts` — add GEMINI_API_KEY, OPENAI_API_KEY bindings
- Modify: `worker/src/triage.ts` — use provider factory instead of direct Anthropic call
- Modify: `worker/src/communicate.ts` — same

- [ ] Define `LLMProvider` interface: `complete(prompt, options) → string`
- [ ] Extract current Anthropic logic into `anthropic.ts`
- [ ] Implement `gemini.ts` — Gemini 2.5 Flash via `generativelanguage.googleapis.com`
- [ ] Implement `openai.ts` — GPT-4.1-mini via `api.openai.com`
- [ ] Create `provider.ts` factory with ordered fallback: Claude → Gemini → OpenAI
- [ ] Add timeout + retry logic: 10s timeout per provider, move to next on failure
- [ ] Update triage.ts and communicate.ts to use factory

### Task 4: Tests for multi-LLM fallback

- [ ] Test: primary provider succeeds → returns result
- [ ] Test: primary fails → falls back to secondary
- [ ] Test: all providers fail → returns error gracefully
- [ ] Test: timeout triggers fallback

### Task 5: Deploy + set secrets

- [ ] `wrangler secret put GEMINI_API_KEY`
- [ ] `wrangler secret put OPENAI_API_KEY`
- [ ] Deploy worker
- [ ] Test by temporarily setting invalid Anthropic key → verify fallback
- [ ] Close #213 and adrianwedd/adrianwedd-ops#12

---

## Phase 3: Deferred / Not This Sprint

| Item | Why deferred |
|------|-------------|
| #189 Facebook renewal | Not due until June 2026, auto-monitored |
| #187 Instagram cross-posting | Needs Meta scope approval (lead time) |
| #188 Bluesky AT Protocol | Additive, lower priority than resilience |
| Atomic rate limiting (Durable Objects) | Over-engineered for current traffic |
| CSP nonces | Needs build-time nonce injection — architectural change |

---

## Exit Criteria

- All 22 infographics at 1536x2752 portrait branded format
- Enquiry confirmation emails confirmed working (or diagnosed if CF Email Routing needs setup)
- LLM provider fallback chain live: Claude → Gemini → GPT-4.1-mini
- All related GitHub issues closed with evidence
