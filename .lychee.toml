# Lychee link checker config
# https://lychee.cli.rs/

# Accept these HTTP status codes as valid
accept = [200, 206, 301, 302, 303, 307, 308]

# Timeout per request in seconds
timeout = 10

# Number of retries
max_retries = 2

# Maximum number of concurrent requests
max_concurrency = 8

# Exclude these URLs/patterns from checking
exclude = [
  # Login-walled or rate-limited
  "https?://(www\\.)?linkedin\\.com",
  "https?://(www\\.)?twitter\\.com",
  "https?://x\\.com",
  "https?://(www\\.)?facebook\\.com",
  "https?://(www\\.)?instagram\\.com",
  # Archive sites (can be slow/flaky)
  "https?://web\\.archive\\.org",
  "https?://archive\\.today",
  # Localhost
  "http://localhost",
  "http://127\\.0\\.0\\.1",
  # Schema/vocab URLs (not real pages)
  "https?://schema\\.org",
  "https?://www\\.w3\\.org",
  # Non-http schemes
  "^mailto:",
  "^tel:",
  # Build artifacts — Astro hashed asset paths are not content links
  "/_astro/",
  # Returns 403 for bots
  "https?://claude\\.ai",
  # Private GitHub repos (exist but return 404 to unauthenticated requests)
  "https://github\\.com/adrianwedd/failure-first-embodied-ai",
  "https://github\\.com/adrianwedd/VERITAS",
  "https://github\\.com/adrianwedd/orchestrix",
]

# Exclude these file patterns from being checked AS SOURCES
# (we check dist/ output, not src/)
exclude_path = []
